<UserControl x:Class="CodeSnip.Views.HighlightingEditorView.HighlightingEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:avalonEdit="http://icsharpcode.net/sharpdevelop/avalonedit"
             xmlns:local="clr-namespace:CodeSnip.Views.HighlightingEditorView"
             xmlns:behaviors="clr-namespace:Microsoft.Xaml.Behaviors;assembly=Microsoft.Xaml.Behaviors"
             xmlns:helpers="clr-namespace:CodeSnip.Helpers"
             d:DataContext="{d:DesignInstance Type={x:Type local:HighlightingEditorViewModel}}"
             x:Name="flyHighlightingEditor"               
             mc:Ignorable="d" 
             d:DesignHeight="400"
             d:DesignWidth="300" Loaded="FlyHighlightingEditor_Loaded">
    <UserControl.InputBindings>
        <KeyBinding Key="K" Modifiers="Control" Command="{Binding ToggleCommentCommand}" />
        <KeyBinding Key="F" Modifiers="Control" Command="{Binding FormatXshdCommand}" />
    </UserControl.InputBindings>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding Message}" FontWeight="Bold" Margin="0,0,0,10" />

        <mah:MetroTabControl Grid.Row="1"
                             Style="{DynamicResource MahApps.Styles.TabControl.Animated}" 
                             mah:HeaderedControlHelper.HeaderFontSize="18" >
            <mah:MetroTabItem Name="tabVisual" Header="Visual Editor">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <ListBox Grid.Row="0"
                             ItemsSource="{Binding HighlightingColors}"
                             SelectedItem="{Binding SelectedColor, Mode=TwoWay}"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="{DynamicResource MahApps.Brushes.Gray3}" BorderThickness="0,0,0,1" Padding="2">
                                    <Grid VerticalAlignment="Center">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="2*" />
                                            <!-- Name -->
                                            <ColumnDefinition Width="Auto" />
                                            <!-- Rectangle -->
                                            <ColumnDefinition Width="Auto" />
                                            <!-- Weight label and combobox -->
                                            <ColumnDefinition Width="Auto" />
                                            <!-- Style label and combobox -->
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Text="{Binding Name}" Grid.Column="0" VerticalAlignment="Center" 
                                                   TextTrimming="CharacterEllipsis" ToolTip="{Binding Name}" />

                                        <Rectangle Width="16" Height="16" Margin="5,0,0,0" VerticalAlignment="Center" Grid.Column="1">
                                            <Rectangle.Fill>
                                                <SolidColorBrush Color="{Binding Foreground, FallbackValue=Transparent}" />
                                            </Rectangle.Fill>
                                        </Rectangle>

                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0,0,0" Grid.Column="2">
                                            <TextBlock Text="Weight:" VerticalAlignment="Center" Margin="0,0,5,0" />
                                            <ComboBox Width="80"
                                                      SelectedItem="{Binding FontWeight, Mode=TwoWay}"
                                                      ItemsSource="{Binding AvailableFontWeights}"
                                                      IsEditable="False"
                                                      VerticalAlignment="Center">
                                                <ComboBox.ItemTemplate>
                                                    <DataTemplate>
                                                        <TextBlock Text="{Binding}" />
                                                    </DataTemplate>
                                                </ComboBox.ItemTemplate>
                                            </ComboBox>
                                        </StackPanel>

                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0,0,0" Grid.Column="3">
                                            <TextBlock Text="Style:" VerticalAlignment="Center" Margin="0,0,5,0" />
                                            <ComboBox Width="80"
                                                      SelectedItem="{Binding FontStyle, Mode=TwoWay}"
                                                      ItemsSource="{Binding AvailableFontStyles}"
                                                      IsEditable="False"
                                                      VerticalAlignment="Center">
                                                <ComboBox.ItemTemplate>
                                                    <DataTemplate>
                                                        <TextBlock Text="{Binding}" />
                                                    </DataTemplate>
                                                </ComboBox.ItemTemplate>
                                            </ComboBox>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                    <StackPanel Grid.Row="1" Margin="0,10,0,0" Orientation="Horizontal">
                        <TextBlock Text="Foreground Color:" FontWeight="Bold" Margin="0,0,10,0"/>
                        <mah:ColorPicker SelectedColor="{Binding SelectedColor.Foreground, Mode=TwoWay}" Width="200" />
                    </StackPanel>
                </Grid>
            </mah:MetroTabItem>
            <mah:MetroTabItem Name="tabXSHD" Header="XSHD Source">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top"
                               Text="Warning: Incorrectly editing the XSHD code can cause syntax highlighting errors and affect application functionality."
                               Foreground="Tomato"
                               TextWrapping="Wrap"
                               Margin="0,0,0,10" />
                    <avalonEdit:TextEditor
                        x:Name="XshdEditor"
                        SyntaxHighlighting="{Binding XmlHighlighting}"
                        Foreground="{Binding XshdEditorForeground}"
                        FontFamily="Consolas"
                        FontSize="12"
                        ShowLineNumbers="True"
                        VerticalScrollBarVisibility="Auto"
                        HorizontalScrollBarVisibility="Auto"
                        WordWrap="False">
                        <avalonEdit:TextEditor.Options>
                            <avalonEdit:TextEditorOptions EnableEmailHyperlinks="False"
                                                          EnableHyperlinks="False"
                                                          HighlightCurrentLine="True"
                                                          ConvertTabsToSpaces="True"
                                                          IndentationSize="4"/>
                        </avalonEdit:TextEditor.Options>
                        <avalonEdit:TextEditor.ContextMenu>
                            <ContextMenu FontSize="11">
                                <MenuItem Header="Cut" Command="ApplicationCommands.Cut">
                                    <MenuItem.Icon>
                                        <Path Data="{StaticResource CutMD}" Fill="{DynamicResource MahApps.Brushes.AccentBase}"
                                              Width="16" Height="16" Stretch="Uniform" />
                                    </MenuItem.Icon>
                                </MenuItem>
                                <MenuItem Header="Copy" Command="ApplicationCommands.Copy">
                                    <MenuItem.Icon>
                                        <Path Data="{StaticResource CopyMD}" Fill="{DynamicResource MahApps.Brushes.AccentBase}"
                                              Width="16" Height="16" Stretch="Uniform" />
                                    </MenuItem.Icon>
                                </MenuItem>
                                <MenuItem Header="Paste" Command="ApplicationCommands.Paste">
                                    <MenuItem.Icon>
                                        <Path Data="{StaticResource Paste}" Fill="{DynamicResource MahApps.Brushes.AccentBase}"
                                              Width="16" Height="16" Stretch="Uniform" />
                                    </MenuItem.Icon>
                                </MenuItem>
                                <Separator />
                                <MenuItem Header="Undo" Command="ApplicationCommands.Undo">
                                    <MenuItem.Icon>
                                        <Path Data="{StaticResource UndoMD}" Fill="{DynamicResource MahApps.Brushes.AccentBase}"
                                              Width="16" Height="16" Stretch="Uniform" />
                                    </MenuItem.Icon>
                                </MenuItem>
                                <MenuItem Header="Redo" Command="ApplicationCommands.Redo">
                                    <MenuItem.Icon>
                                        <Path Data="{StaticResource RedoMD}" Fill="{DynamicResource MahApps.Brushes.AccentBase}"
                                              Width="16" Height="16" Stretch="Uniform" />
                                    </MenuItem.Icon>
                                </MenuItem>
                                <Separator />
                                <MenuItem Header="SelectAll" Command="ApplicationCommands.SelectAll">
                                    <MenuItem.Icon>
                                        <Path Data="{StaticResource SelectAll}" Fill="{DynamicResource MahApps.Brushes.AccentBase}"
                                              Width="16" Height="16" Stretch="Uniform" />
                                    </MenuItem.Icon>
                                </MenuItem>
                                <Separator />
                                <MenuItem Header="Comment" Command="{Binding ToggleCommentCommand}" InputGestureText="Ctrl+K">
                                    <MenuItem.Icon>
                                        <Path Data="{StaticResource CommentTextMultipleThin}" Fill="{DynamicResource MahApps.Brushes.AccentBase}"
                                              Width="16" Height="16" Stretch="Uniform" />
                                    </MenuItem.Icon>
                                </MenuItem>
                                <MenuItem Header="Format" Command="{Binding FormatXshdCommand}" InputGestureText="Ctrl+F">
                                    <MenuItem.Icon>
                                        <Path Data="{StaticResource FormatText}" Fill="{DynamicResource MahApps.Brushes.AccentBase}"
                                              Width="16" Height="16" Stretch="Uniform" />
                                    </MenuItem.Icon>
                                </MenuItem>
                                <MenuItem Header="Wrap Lines"
                                          IsCheckable="True"         
                                          IsChecked="{Binding PlacementTarget.WordWrap, RelativeSource={RelativeSource AncestorType=ContextMenu}, Mode=TwoWay}">
                                    <MenuItem.Icon>
                                        <Path Data="{StaticResource WrapText}" Fill="{DynamicResource MahApps.Brushes.AccentBase}"
                                              Width="16" Height="16" Stretch="Uniform" />
                                    </MenuItem.Icon>
                                </MenuItem>
                            </ContextMenu>
                        </avalonEdit:TextEditor.ContextMenu>
                        <behaviors:Interaction.Behaviors>
                            <helpers:AvalonEditTextBindingBehavior BoundText="{Binding XshdText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                        </behaviors:Interaction.Behaviors>
                    </avalonEdit:TextEditor>
                </DockPanel>
            </mah:MetroTabItem>
        </mah:MetroTabControl>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,20,0,0">
            <Button Content="Apply Preview" Margin="0,0,10,0" Command="{Binding ApplyLivePreviewCommand}" />
            <mah:Badged Badge="{Binding SyncBadge}" BadgePlacementMode="TopRight"  Margin="0,0,10,0"  BadgeBackground="{Binding SyncBadgeBackground}">
                <Button Content="Sync xshd -&gt; visual" Command="{Binding SyncFromRawCommand}" ToolTip="Sync Color elements from xshd source"/>
            </mah:Badged>
            <Button Content="Reset to Default" Margin="0,0,10,0" Command="{Binding ResetDefinitionCommand}" />
            <Button Content="Save" Width="80" Margin="0,0,10,0" Command="{Binding SaveCommand}" />
        </StackPanel>
    </Grid>
</UserControl>