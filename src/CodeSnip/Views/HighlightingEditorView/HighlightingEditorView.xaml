<UserControl x:Class="CodeSnip.Views.HighlightingEditorView.HighlightingEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CodeSnip.Views.HighlightingEditorView"
             xmlns:behaviors="clr-namespace:Microsoft.Xaml.Behaviors;assembly=Microsoft.Xaml.Behaviors"
             xmlns:helpers="clr-namespace:CodeSnip.Helpers"
             x:Name="flyHighlightingEditor"
             MinWidth="500"
             MaxWidth="600"
             mah:HeaderedControlHelper.HeaderMargin="10"
             mc:Ignorable="d" 
             d:DesignHeight="400"
             d:DesignWidth="300">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="{Binding Message}" FontWeight="Bold" Margin="0,0,0,10" />
        <ListBox Grid.Row="1"
         ItemsSource="{Binding HighlightingColors}"
         SelectedItem="{Binding SelectedColor, Mode=TwoWay}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="LightGray" BorderThickness="0,0,0,1" Padding="2">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" >
                            <TextBlock Text="{Binding Name}" Width="120" VerticalAlignment="Center" />
                            <Rectangle Width="16" Height="16" Margin="5,0,0,0" VerticalAlignment="Center">
                                <Rectangle.Fill>
                                    <SolidColorBrush Color="{Binding Foreground}" />
                                </Rectangle.Fill>
                            </Rectangle>

                            <!-- Label i ComboBox za FontWeight -->
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0,0,0">
                                <TextBlock Text="Weight:" VerticalAlignment="Center" Margin="0,0,5,0" />
                                <ComboBox Width="80"
                                      SelectedItem="{Binding FontWeight, Mode=TwoWay}"
                                      ItemsSource="{Binding AvailableFontWeights}"
                                      IsEditable="False"
                                      VerticalAlignment="Center">
                                    <ComboBox.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding}" />
                                        </DataTemplate>
                                    </ComboBox.ItemTemplate>
                                </ComboBox>
                            </StackPanel>
                            <!-- Label i ComboBox za FontStyle -->
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0,0,0">
                                <TextBlock Text="Style:" VerticalAlignment="Center" Margin="0,0,5,0" />
                                <ComboBox Width="80"
                                      SelectedItem="{Binding FontStyle, Mode=TwoWay}"
                                      ItemsSource="{Binding AvailableFontStyles}"
                                      IsEditable="False"
                                      VerticalAlignment="Center">
                                    <ComboBox.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding}" />
                                        </DataTemplate>
                                    </ComboBox.ItemTemplate>
                                </ComboBox>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <StackPanel Grid.Row="2" Margin="0,10,0,10" Orientation="Horizontal">
            <TextBlock Text="Foreground Color:" FontWeight="Bold" Margin="0,0,10,0"/>
            <mah:ColorPicker SelectedColor="{Binding SelectedColor.Foreground, Mode=TwoWay}" Width="200" />
        </StackPanel>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,10,0,0">
            <Button Content="Apply Preview" Margin="0,0,10,0" Command="{Binding ApplyLivePreviewCommand}" />
            <Button Content="Reset to Default" Margin="0,0,10,0" Command="{Binding ResetDefinitionCommand}" />
			<Button Content="Save" Width="80" Margin="0,0,10,0" Command="{Binding SaveCommand}" />
        </StackPanel>
    </Grid>
</UserControl>