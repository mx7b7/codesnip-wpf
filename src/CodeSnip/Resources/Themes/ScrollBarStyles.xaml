<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls" 
                    xmlns:options="http://schemas.microsoft.com/winfx/2006/xaml/presentation/options" 
                    xmlns:system="clr-namespace:System;assembly=mscorlib">
    <system:Double x:Key="CodeSnip.Sizes.ScrollBar.Height">18</system:Double>
    <system:Double x:Key="CodeSnip.Sizes.ScrollBar.Width">18</system:Double>
    <QuinticEase x:Key="ScrollBarEaseInOut" options:Freeze="true" EasingMode="EaseInOut" />

    <ControlTemplate x:Key="CodeSnip.Templates.ScrollBar.Horizontal" TargetType="{x:Type ScrollBar}">
        <Grid Height="{TemplateBinding FrameworkElement.Height}">
            <Grid Name="HorizontalRoot" Height="3" VerticalAlignment="Center" Background="#00FFFFFF">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="0.00001*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <RepeatButton Name="SmallDecrease" Grid.Column="0" Command="ScrollBar.LineLeftCommand" Content="M54,14.9765625L54,74.9765625 24,44.9765625 54,14.9765625z" Opacity="0" Style="{DynamicResource MahApps.Styles.RepeatButton.ScrollBarSmall}" Visibility="Visible" />
                <Track Name="PART_Track" Grid.Column="1" IsDirectionReversed="False">
                    <Track.DecreaseRepeatButton>
                        <RepeatButton Name="LargeDecrease" Command="ScrollBar.PageLeftCommand" Style="{DynamicResource MahApps.Styles.RepeatButton.ScrollBarLarge}" />
                    </Track.DecreaseRepeatButton>
                    <Track.Thumb>
                        <mah:MetroThumb Margin="0 1 0 1" Style="{DynamicResource MahApps.Styles.Thumb.ScrollBar}" />
                    </Track.Thumb>
                    <Track.IncreaseRepeatButton>
                        <RepeatButton Name="LargeIncrease" Command="ScrollBar.PageRightCommand" Style="{DynamicResource MahApps.Styles.RepeatButton.ScrollBarLarge}" />
                    </Track.IncreaseRepeatButton>
                </Track>
                <RepeatButton Name="SmallIncrease" Grid.Column="2" Command="ScrollBar.LineRightCommand" Content="M192,79.875L352,239.875 192,399.875 192,79.875z" Opacity="0" Style="{DynamicResource MahApps.Styles.RepeatButton.ScrollBarSmall}" />
            </Grid>
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="UIElement.IsEnabled" Value="False">
                <Setter TargetName="HorizontalRoot" Property="UIElement.Opacity" Value="0.5" />
            </Trigger>
            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Value="True" Binding="{Binding Path=IsVisible, RelativeSource={RelativeSource Self}}" />
                    <Condition Value="True" Binding="{Binding Path=IsMouseOver, RelativeSource={RelativeSource Self}}" />
                </MultiDataTrigger.Conditions>
                <TriggerBase.EnterActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation FillBehavior="HoldEnd" Storyboard.TargetName="HorizontalRoot" Storyboard.TargetProperty="Height" Duration="0:0:0.2" EasingFunction="{StaticResource ScrollBarEaseInOut}" To="{StaticResource CodeSnip.Sizes.ScrollBar.Width}" />
                            <DoubleAnimation FillBehavior="HoldEnd" Storyboard.TargetName="SmallIncrease" Storyboard.TargetProperty="Opacity" To="1" Duration="0" />
                            <DoubleAnimation FillBehavior="HoldEnd" Storyboard.TargetName="SmallDecrease" Storyboard.TargetProperty="Opacity" To="1" Duration="0" />
                        </Storyboard>
                    </BeginStoryboard>
                </TriggerBase.EnterActions>
                <TriggerBase.ExitActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation FillBehavior="HoldEnd" Storyboard.TargetName="HorizontalRoot" Storyboard.TargetProperty="Height" To="3" Duration="0:0:0.2" EasingFunction="{StaticResource ScrollBarEaseInOut}" />
                            <DoubleAnimation FillBehavior="HoldEnd" Storyboard.TargetName="SmallIncrease" Storyboard.TargetProperty="Opacity" To="0" Duration="0" />
                            <DoubleAnimation FillBehavior="HoldEnd" Storyboard.TargetName="SmallDecrease" Storyboard.TargetProperty="Opacity" To="0" Duration="0" />
                        </Storyboard>
                    </BeginStoryboard>
                </TriggerBase.ExitActions>
            </MultiDataTrigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
    <ControlTemplate x:Key="CodeSnip.Templates.ScrollBar.Vertical" TargetType="{x:Type ScrollBar}">
        <Grid Width="{TemplateBinding FrameworkElement.Width}" Background="#00FFFFFF" >
            <Grid Name="VerticalRoot" Width="3" HorizontalAlignment="Right">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="0.00001*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <RepeatButton Name="SmallDecrease" Grid.Row="0" Command="ScrollBar.LineUpCommand" Content="M240.125,160L400.125,320 80.125,320 240.125,160z" Opacity="0" Style="{DynamicResource MahApps.Styles.RepeatButton.ScrollBarSmall}" Visibility="Visible" />
                <Track Name="PART_Track" Grid.Row="1" IsDirectionReversed="True">
                    <FrameworkElement.Resources>
                        <ResourceDictionary>
                            <system:Double x:Key="{x:Static SystemParameters.VerticalScrollBarButtonHeightKey}">50</system:Double>
                        </ResourceDictionary>
                    </FrameworkElement.Resources>
                    <Track.DecreaseRepeatButton>
                        <RepeatButton Name="VerticalLargeDecrease" Command="ScrollBar.PageUpCommand" Style="{DynamicResource MahApps.Styles.RepeatButton.ScrollBarLarge}" />
                    </Track.DecreaseRepeatButton>
                    <Track.Thumb>
                        <mah:MetroThumb Margin="1 0 1 0" Style="{DynamicResource MahApps.Styles.Thumb.ScrollBar}" />
                    </Track.Thumb>
                    <Track.IncreaseRepeatButton>
                        <RepeatButton Name="VerticalLargeIncrease" Command="ScrollBar.PageDownCommand" Style="{DynamicResource MahApps.Styles.RepeatButton.ScrollBarLarge}" />
                    </Track.IncreaseRepeatButton>
                </Track>
                <RepeatButton Name="SmallIncrease" Grid.Row="2" Command="ScrollBar.LineDownCommand" Content="M80.125,192L400.125,192 240.125,352 80.125,192z" Opacity="0" Style="{DynamicResource MahApps.Styles.RepeatButton.ScrollBarSmall}" Visibility="Visible" />
            </Grid>
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="UIElement.IsEnabled" Value="False">
                <Setter TargetName="VerticalRoot" Property="UIElement.Opacity" Value="0.5" />
            </Trigger>
            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Value="True" Binding="{Binding Path=IsVisible, RelativeSource={RelativeSource Self}}" />
                    <Condition Value="True" Binding="{Binding Path=IsMouseOver, RelativeSource={RelativeSource Self}}" />
                </MultiDataTrigger.Conditions>
                <TriggerBase.EnterActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation FillBehavior="HoldEnd" Storyboard.TargetName="VerticalRoot" Storyboard.TargetProperty="Width" Duration="0:0:0.2" EasingFunction="{StaticResource ScrollBarEaseInOut}" To="{StaticResource CodeSnip.Sizes.ScrollBar.Width}" />
                            <DoubleAnimation FillBehavior="HoldEnd" Storyboard.TargetName="SmallIncrease" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.2" />
                            <DoubleAnimation FillBehavior="HoldEnd" Storyboard.TargetName="SmallDecrease" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.2" />
                        </Storyboard>
                    </BeginStoryboard>
                </TriggerBase.EnterActions>
                <TriggerBase.ExitActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation FillBehavior="HoldEnd" Storyboard.TargetName="VerticalRoot" Storyboard.TargetProperty="Width" To="3" Duration="0:0:0.5" EasingFunction="{StaticResource ScrollBarEaseInOut}" />
                            <DoubleAnimation FillBehavior="HoldEnd" Storyboard.TargetName="SmallIncrease" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.5" />
                            <DoubleAnimation FillBehavior="HoldEnd" Storyboard.TargetName="SmallDecrease" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.5" />
                        </Storyboard>
                    </BeginStoryboard>
                </TriggerBase.ExitActions>
            </MultiDataTrigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <!-- Global style for ScrollBar -->
    <Style x:Key="DefaultScrollBar" TargetType="{x:Type ScrollBar}" BasedOn="{StaticResource MahApps.Styles.ScrollBar}">
        <Style.Triggers>
            <Trigger Property="Orientation" Value="Horizontal">
                <Setter Property="Width" Value="Auto" />
                <Setter Property="Height" Value="10" />
            </Trigger>
            <Trigger Property="Orientation" Value="Vertical">
                <Setter Property="Width" Value="10" />
                <Setter Property="Height" Value="Auto" />
            </Trigger>
            <DataTrigger
                Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsMouseOver}"
                Value="False">
                <Setter Property="Opacity" Value="0.25" />
            </DataTrigger>
            <DataTrigger
                Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsMouseOver}"
                Value="True">
                <Setter Property="Opacity" Value="0.40" />
            </DataTrigger>
        </Style.Triggers>
    </Style>
    <!-- Style for ScrollBar inside TreeView -->
    <Style x:Key="TreeViewScrollBar" TargetType="{x:Type ScrollBar}" BasedOn="{StaticResource DefaultScrollBar}">
        <Style.Triggers>
            <Trigger Property="Orientation" Value="Horizontal">
                <Setter Property="Height" Value="6" />
            </Trigger>
            <Trigger Property="Orientation" Value="Vertical">
                <Setter Property="Width" Value="6" />
            </Trigger>
        </Style.Triggers>
    </Style>
    <!-- Style for avalonEdit ScrollBar -->
    <Style x:Key="AutoHideScrollBar" TargetType="{x:Type ScrollBar}">
        <Setter Property="FrameworkElement.OverridesDefaultStyle" Value="True" />
        <Setter Property="UIElement.SnapsToDevicePixels" Value="True" />
        <Style.Triggers>
            <Trigger Property="ScrollBar.Orientation" Value="Horizontal">
                <Setter Property="Height" Value="{DynamicResource CodeSnip.Sizes.ScrollBar.Height}" />
                <Setter Property="Template" Value="{DynamicResource CodeSnip.Templates.ScrollBar.Horizontal}" />
                <Setter Property="Width" Value="Auto" />
            </Trigger>
            <Trigger Property="ScrollBar.Orientation" Value="Vertical">
                <Setter Property="Height" Value="Auto" />
                <Setter Property="Template" Value="{DynamicResource CodeSnip.Templates.ScrollBar.Vertical}" />
                <Setter Property="Width" Value="{DynamicResource CodeSnip.Sizes.ScrollBar.Width}" />
            </Trigger>
            <DataTrigger
                Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsMouseOver}"
                Value="False">
                <Setter Property="Opacity" Value="0.35" />
            </DataTrigger>
            <DataTrigger
                Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsMouseOver}"
                Value="True">
                <Setter Property="Opacity" Value="0.50" />
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="{x:Type TypeName=ScrollBar}" BasedOn="{StaticResource ResourceKey=DefaultScrollBar}" />
</ResourceDictionary>